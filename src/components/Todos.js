import TodoItem from './TodoItem';

const Todos = () => {
  return (
    <ul>
      <TodoItem text="Learn React Native" />
      <TodoItem text="Learn React Master Class" />
    </ul>
  );
};

export default Todos;
